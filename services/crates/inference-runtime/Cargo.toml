[package]
name    = "inference-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
disruptor  = { path = "../disruptor-rs" }
serde      = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror  = "1.0"
