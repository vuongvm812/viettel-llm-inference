# Only the pre-built binary, model, and config are COPYed into the image. Send
# nothing else to the daemon — keeps the context tiny and source out of the build.
*
!dist/inference-runtime
!models/qwen3.5-2b-bf16.gguf
!config/default-config.yaml
