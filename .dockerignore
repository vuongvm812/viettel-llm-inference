# Only the pre-built binary and config are COPYed into the image (the model is
# bind-mounted at /model, not baked in). Send nothing else to the daemon — keeps
# the context tiny and source out of the build.
*
!dist/inference-runtime
!config/default-config.yaml
