diff --git a/Cargo.toml b/Cargo.toml
index 435350c..3a6c246 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -152,5 +152,7 @@ serde_json = { opt-level = 3 }
 tokenizers = { opt-level = 3 }
 
 [profile.release]
-lto = "thin"
+opt-level = 3
+lto = "fat"
+codegen-units = 1
 panic = "abort"
diff --git a/src/server/Cargo.toml b/src/server/Cargo.toml
index f3e0386..5cb1044 100644
--- a/src/server/Cargo.toml
+++ b/src/server/Cargo.toml
@@ -24,6 +24,7 @@ prost-types.workspace = true
 rmpv.workspace = true
 serde.workspace = true
 serde_json.workspace = true
+sonic-rs = "0.5"
 serde_with.workspace = true
 sha2.workspace = true
 socket2.workspace = true
diff --git a/src/server/src/routes/openai/utils/validated_json.rs b/src/server/src/routes/openai/utils/validated_json.rs
index d07158c..e76597e 100644
--- a/src/server/src/routes/openai/utils/validated_json.rs
+++ b/src/server/src/routes/openai/utils/validated_json.rs
@@ -1,9 +1,9 @@
 //! Validated JSON extractor for automatic request validation.
 //! Variation of https://github.com/lightseekorg/smg/blob/main/crates/protocols/src/validated.rs
 
-use axum::Json;
-use axum::extract::rejection::JsonRejection;
+use axum::body::Bytes;
 use axum::extract::{FromRequest, Request};
+use axum::http::{HeaderMap, header};
 use serde::de::DeserializeOwned;
 use validator::Validate;
 
@@ -17,6 +17,13 @@ use crate::error::{ApiError, invalid_request};
 /// `.validate()` on types that implement the `Validate` trait. If validation
 /// fails, it returns [`ApiError::InvalidRequest`] with details about the
 /// validation errors.
+///
+/// Deserialization uses `sonic_rs` (SIMD-accelerated) rather than axum's
+/// serde_json-backed `Json` extractor. sonic_rs parses from an immutable slice,
+/// so we borrow the body `Bytes` directly — no copy. `Value` fields inside
+/// request types stay `serde_json::Value` — sonic_rs's serde bridge handles
+/// those subtrees, so nothing downstream (templates, schema) changes. Like
+/// axum's `Json`, a non-JSON `Content-Type` is rejected before the body is read.
 pub struct ValidatedJson<T>(pub T);
 
 impl<S, T> FromRequest<S> for ValidatedJson<T>
@@ -27,9 +34,17 @@ where
     type Rejection = ApiError;
 
     async fn from_request(req: Request, state: &S) -> Result<Self, Self::Rejection> {
-        let Json(mut data) = Json::<T>::from_request(req, state)
+        if !is_json_content_type(req.headers()) {
+            return Err(ApiError::json_parse_error(
+                "expected request with `Content-Type: application/json`",
+            ));
+        }
+
+        let bytes = Bytes::from_request(req, state)
             .await
-            .map_err(|err: JsonRejection| ApiError::json_parse_error(err.body_text()))?;
+            .map_err(|err| ApiError::json_parse_error(err.body_text()))?;
+
+        let mut data: T = parse_json(&bytes)?;
 
         data.normalize();
 
@@ -40,6 +55,27 @@ where
     }
 }
 
+/// Deserialize `T` from the JSON body with sonic_rs (SIMD-accelerated). sonic_rs
+/// parses from an immutable slice, so we borrow the body bytes directly — no copy.
+fn parse_json<T: DeserializeOwned>(bytes: &[u8]) -> Result<T, ApiError> {
+    sonic_rs::from_slice(bytes)
+        .map_err(|err| ApiError::json_parse_error(err.to_string()))
+}
+
+/// Accept `application/json` and `application/<x>+json` (with optional params),
+/// matching what axum's `Json` extractor required before we replaced it.
+fn is_json_content_type(headers: &HeaderMap) -> bool {
+    let Some(ct) = headers.get(header::CONTENT_TYPE).and_then(|v| v.to_str().ok()) else {
+        return false;
+    };
+    let essence = ct.split(';').next().unwrap_or("").trim();
+    const PREFIX: &str = "application/";
+    essence.eq_ignore_ascii_case("application/json")
+        || (essence.len() > PREFIX.len()
+            && essence[..PREFIX.len()].eq_ignore_ascii_case(PREFIX)
+            && essence.to_ascii_lowercase().ends_with("+json"))
+}
+
 impl<T> std::ops::Deref for ValidatedJson<T> {
     type Target = T;
 
@@ -53,3 +89,67 @@ impl<T> std::ops::DerefMut for ValidatedJson<T> {
         &mut self.0
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use axum::http::{HeaderMap, HeaderValue, header};
+    use serde::Deserialize;
+    use serde_json::json;
+
+    use super::{is_json_content_type, parse_json};
+
+    #[derive(Debug, PartialEq, Deserialize)]
+    struct Sample {
+        model: String,
+        messages: Vec<serde_json::Value>,
+        max_tokens: Option<u32>,
+    }
+
+    // sonic_rs parse must match serde_json on a representative chat body,
+    // including an embedded serde_json::Value subtree (messages).
+    #[test]
+    fn sonic_rs_matches_serde_json() {
+        let body = json!({
+            "model": "test-model",
+            "messages": [
+                {"role": "system", "content": "You are helpful."},
+                {"role": "user", "content": "Hi"}
+            ],
+            "max_tokens": 128
+        })
+        .to_string();
+
+        let via_sonic: Sample = parse_json(body.as_bytes()).unwrap();
+        let via_serde: Sample = serde_json::from_slice(body.as_bytes()).unwrap();
+        assert_eq!(via_sonic, via_serde);
+        assert_eq!(via_sonic.model, "test-model");
+        assert_eq!(via_sonic.messages.len(), 2);
+        assert_eq!(via_sonic.max_tokens, Some(128));
+    }
+
+    #[test]
+    fn invalid_json_is_an_error() {
+        let bad = b"{not json";
+        assert!(parse_json::<Sample>(bad).is_err());
+    }
+
+    fn headers_with_content_type(value: &str) -> HeaderMap {
+        let mut headers = HeaderMap::new();
+        headers.insert(header::CONTENT_TYPE, HeaderValue::from_str(value).unwrap());
+        headers
+    }
+
+    #[test]
+    fn content_type_guard_matches_axum_json() {
+        assert!(is_json_content_type(&headers_with_content_type("application/json")));
+        assert!(is_json_content_type(&headers_with_content_type(
+            "application/json; charset=utf-8"
+        )));
+        assert!(is_json_content_type(&headers_with_content_type("APPLICATION/JSON")));
+        assert!(is_json_content_type(&headers_with_content_type("application/ld+json")));
+
+        assert!(!is_json_content_type(&headers_with_content_type("text/plain")));
+        assert!(!is_json_content_type(&headers_with_content_type("application/xml")));
+        assert!(!is_json_content_type(&HeaderMap::new())); // missing header
+    }
+}
